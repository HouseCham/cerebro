package routes

import "github.com/gofiber/fiber/v3"

// SetAllRoutes sets all routes for the API Endpoints
func SetAllRoutes(app *fiber.App) {
	// Costumer routes
	// costumerRoutes := app.Group("/api/v1/costumer")
	// costumerRoutes.Post("/", controllers.InsertNewCostumer)
}